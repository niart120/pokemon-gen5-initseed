/**
 * レスポンシブUIスケーリング用のサイズクラス生成ユーティリティ
 * 1080p/100%環境を基準（スケール=1.0）として、
 * 他の解像度環境に適応的にスケーリングを適用
 */

// Deprecated: use '@/lib/utils/responsive-sizes' instead
export type { ResponsiveSizes } from '@/lib/utils/responsive-sizes';
export { getResponsiveSizes, getSizeCategory } from '@/lib/utils/responsive-sizes';
